cachesystem.py:实现一个cache装饰器 和 key过期被清除功能
实现以下参数形式 均被缓存的效果
add(4,5,z=6)
add(4,y=5,z=6)
add(4,5,6)
add(x=4,z=6,y=5)
add(4,5)

key过期清除:每次执行add函数 需要先判断key是否过期 过期则清除掉
如果cache字典不过期 在执行查找缓存功能

command.py:命令分发装饰器
有注册函数到某一个命令功能
用户输入命令 需要路由到某一个命令 执行命令函数
如果输入的命令不存在  执行默认函数 

